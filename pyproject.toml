[project]
name = "sec-financial-intelligence"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  # ingestion pipeline
  "google-cloud-bigquery>=3.25",
  "pyyaml>=6.0",
  "python-dotenv>=1.0",
  "requests>=2.32",
  "tenacity>=9.0",
  "click>=8.1",
  # dbt
  "dbt-bigquery>=1.11",
]

